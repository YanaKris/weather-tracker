# Конфигурация Ruff — линтера и форматтера для Python.
# Запуск:  ruff check .   (искать проблемы)   |   ruff format .   (форматировать)

line-length = 88

[lint]
# Наборы правил: E — стиль (PEP 8), F — вероятные ошибки, I — сортировка импортов
select = ["E", "F", "I"]
